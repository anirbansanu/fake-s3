# Version control
.git
.gitignore

# Virtual environments
.venv
venv
env

# Python cache
__pycache__
*.py[cod]
*.egg-info
.pytest_cache
.mypy_cache
.ruff_cache

# Stored objects (runtime data, mounted as a volume — never baked into the image)
storage

# Editor / IDE
.vscode
.idea

# Docs and Docker files themselves (not needed inside the image)
*.md
Dockerfile
docker-compose.yml
.dockerignore

# Development, tests, packaging, build outputs (server image needs none of it)
tests
packaging
dist
build
requirements-dev.txt
fakes3/gui
fakes3/cli
fakes3.py

# OS cruft and logs
.DS_Store
Thumbs.db
*.log
